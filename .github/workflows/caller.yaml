name: Node CI

on: [push]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
        - name: 'Upload Artifact'
          uses: actions/upload-artifact@v3
          with:
             name: my-artifact
             path: my_file.txt
             retention-days: 5
