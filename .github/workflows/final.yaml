name: scriptrun
on:
  push:
    branches:
      - main
jobs:    
  my_python_script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name : run build script
        run: ./scripts/python.py
        shell: python 
        
