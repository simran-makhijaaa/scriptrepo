name: scriptrun
on:
  push:
    branches:
      - main
#env:
 # myname: simran 
  # gender: female 
jobs: 
  environment_job:
    runs-on: ubuntu-latest
    steps:
      - name: display environment
        run: |
          echo "this is running inside ${{ runner.os }} server"
        shell: bash
          
    
  
#   my_scriptrun_job:
#     runs-on: ubuntu-latest
#     steps: 
#       - uses: actions/checkout@v3
#       - name: Give execute permission
#         run: chmod +x scripts/build.sh
#       - name: Run build script
#         run: ./scripts/build.sh
#         shell: bash    
        
#  add_num_script:
 #   runs-on: ubuntu-latest
  #  - uses: actions/checkout@v3
   #   - uses: actions/setup-python@v4
    #    with:
     #     python-version: '3.10' 
      #- name: Run python script
       # run: python scripts/addnum.py
 
        
