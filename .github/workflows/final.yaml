name: github actions
run-name: message debuging
on:
  push
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: setting debug message
        run: echo "::error file=app.js,line=1,col=5,endColumn=7::Missing semicolon"

