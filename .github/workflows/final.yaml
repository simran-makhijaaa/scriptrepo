name: learn-github-actions
run-name: dependent job
on: push 
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: addnumfile
        uses: actions/upload-artifact@v3
      - run: ./scripts/build.sh
        shell: bash
 
