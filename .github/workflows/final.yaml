name: github-actions
run-name: reuseable
on:
  workflow_call:
    inputs:
      config-path:
        required: true
        type: string
    secrets:
      Token:
        required: true

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
    - name: helo world
      run: |
        echo "hello, ${{ input.username }}"
