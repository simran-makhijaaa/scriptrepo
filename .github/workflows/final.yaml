name: github-actions
on: push
jobs:
  generate-a-secret-output:
    runs-on: ubuntu-latest
    steps:
        name: Generate a secret
        run: |
        env:
          DEVOPS: ${{ secrets.ADD_MASK }}
            echo "$DEVOPS"
      
