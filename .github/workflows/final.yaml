name: scriptrun
on : [push]
jobs:
  my_scriptrun_job:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - name: Run build script
        run: ./scripts/build.sh
        shell: bash
